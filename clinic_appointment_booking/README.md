# Clinic Appointment Booking (NestJS app)

The project README — setup, architecture, concurrency proof, performance
evidence, and interview walkthrough — lives at the repository root:

**[../README.md](../README.md)**

Application source, tests, and npm scripts are in this directory.

```bash
cd clinic_appointment_booking
npm install
npm run start:dev
```

Docker Compose is run from the repository root (`../docker-compose.yml`).
